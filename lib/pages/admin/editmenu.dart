import 'package:flutter/material.dart';

class EditMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Edit Menu Page'),
    );
  }
}