import 'package:flutter/material.dart';

class Edittable extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child:Text('edit Table Page'),
    );
  }
}